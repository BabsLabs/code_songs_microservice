<!doctype html>
<html>
  <head>
    <!-- Style and javascript links -->
  </head>
  <body>
    <!-- Your content here! -->
    <h1>HELLO WORLD!!!!</h1>
    <% @tracks.each do |track| %>
      <p><%= track.title %></p>
    <% end %>
  </body>
</html>